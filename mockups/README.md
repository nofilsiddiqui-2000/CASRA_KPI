# Dashboard mockups

Open **`casra_dashboard_mockup.html`** in a browser (double-click or drag into Chrome/Edge).

Optional interactive charts (requires Plotly):

```powershell
pip install plotly pandas
python mockups/casra_dashboard_mockup.py
```

This writes `casra_dashboard_mockup_interactive.html`.
